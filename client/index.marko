lasso-page name="di" package-path="./browser.json"
<!doctype html>
html
  head
    meta charset="utf-8"
    title -- Discord Injections
    lasso-head
  body
    app url=data.ctx.request.url redirect=data.ctx.response.redirect.bind(data.ctx.response)
    lasso-body
    browser-refresh enabled=true
